{
  "name": "eventwheelz",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview --port 4322",
    "check": "node scripts/check-links.mjs",
    "verify": "astro build && node scripts/check-links.mjs && node scripts/audit.mjs",
    "zip": "npm run verify && node scripts/maak-zip.mjs",
    "publiceer": "SITE_URL=https://kasper0599.github.io/eventwheelz-mockup npm run verify && node scripts/publiceer.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.1"
  }
}
